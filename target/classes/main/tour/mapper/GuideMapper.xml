<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="main.tour.mapper.GuideMapper">

    <select id="queryGuideById" resultType="Guide">
   SELECT id, name, gender, hours, salary FROM public.guide  where id=#{id}
    </select>

    <select id="queryGuides" resultType="Guide">
SELECT id, name, gender, hours, salary FROM public.guide order by id
    </select>

    <insert id="insertGuide" parameterType="Guide">
INSERT INTO public.guide(id, name, gender, hours, salary) VALUES (#{id}, #{name}, #{gender}, #{hours}, #{salary})
    </insert>


    <delete id="deleteGuideById" parameterType="long">
        DELETE FROM public.guide WHERE id=#{id}
    </delete>

</mapper>